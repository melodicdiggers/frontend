query eventBySlug($slug: String!) {
	events(filters: { slug: { eq: $slug } }) {
		data {
			attributes {
				title
				location
				eventDate
				slug
				description
				ticketPrices
				media {
					data {
						attributes {
							url
						}
					}
				}
			}
		}
	}
}
