query eventBySlug($slug: String!) {
	events(filters: { slug: { eq: $slug } }) {
		data {
			attributes {
				title
				location
				eventDate
				slug
				ticketPrices
				media {
					data {
						attributes {
							url
						}
					}
				}
			}
		}
	}
}
