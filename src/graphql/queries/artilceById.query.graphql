query ArticleById($article_slug: StringFilterInput) {
	articles(filters: { article_slug: $article_slug }) {
		data {
			attributes {
				title
				category
				createdAt
				content
				article_slug
			}
		}
	}
}
