query CoffeeBreak {
	articles(filters: { category: { eq: "CoffeeBreak" } }) {
		data {
			attributes {
				title
				content
				category
				article_slug
				createdAt
				mediaArticle {
					data {
						attributes {
							url
						}
					}
				}
			}
		}
	}
}
