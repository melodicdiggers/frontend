query CoffeeBreak {
	articles(filters: { category: { eq: "CoffeeBreak" } }) {
		data {
			attributes {
				title
				category
				article_slug
				createdAt
				mediaArticle {
					data {
						attributes {
							url
						}
					}
				}
			}
		}
	}
}
